# Python test dependencies for VideoStream
# Install: venv/bin/pip install -r requirements.txt
gcovr
numpy
pytest
pytest-cov  # For Python coverage (more reliable than slipcover)
pytest-benchmark
typeguard